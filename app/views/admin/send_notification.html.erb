<%= form_tag(mail_path(@report_id), method: :get, class: "form-inline") do %>
  <%= text_area(:message, nil, class:"form-control form-control-sm mr-3", placeholder: "Message to be mailed to user") %>
  <%= content_tag(:button, "Send mail", type: 'submit', class: 'btn')%>
<% end %>
